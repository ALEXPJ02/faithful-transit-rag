"""MCP tool interface over the realtime and prediction layers.

Exposes the agent's tools through the Model Context Protocol so the same
capabilities are callable from any MCP client, with FastAPI serving the
HTTP surface for the Phase 2 deployment.
"""
