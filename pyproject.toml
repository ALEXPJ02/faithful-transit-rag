[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "transit-rag"
version = "0.1.0"
description = "An agentic RAG system for real-time Sydney public transport Q&A with automated faithfulness evaluation."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Piero Jauregui" }]
keywords = ["rag", "agentic", "gtfs", "public-transport", "llm-evaluation"]

# Core dependencies: everything the retrieval + agent path needs.
# Heavier, optional concerns live in the extras below so CI and the
# collection-only environment stay light.
dependencies = [
    "anthropic>=0.40",
    "voyageai>=0.3",
    "chromadb>=0.5",
    "pypdf>=5.0",
    "python-dotenv>=1.0",
    "pydantic>=2.7",
    "pydantic-settings>=2.3",
    "requests>=2.31",
]

[project.optional-dependencies]
# Realtime + delay collection. Deliberately separate: the poller runs
# unattended (GitHub Actions / an always-on box) and must not drag in
# chromadb or the model stack.
realtime = [
    "gtfs-realtime-bindings>=1.0",
    "protobuf>=4.25",
]
# Phase 1b prediction layer (see docs/02-tech-stack.md).
prediction = [
    "xgboost>=2.1",
    "scikit-learn>=1.5",
    "pandas>=2.2",
    "numpy>=1.26",
    "joblib>=1.4",
]
# MCP tool interface + HTTP surface (Phase 2 deployment target).
serve = [
    "mcp>=1.2",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
]
# Evaluation harness.
evaluation = [
    "ragas>=0.2",
    "pandas>=2.2",
]
dev = [
    "ruff>=0.6",
    "mypy>=1.11",
    "pytest>=8.3",
    "pytest-cov>=5.0",
    "types-requests",
    "types-protobuf",
]

[project.scripts]
transit-poller = "transit_rag.prediction.collection.poller:main"

[project.urls]
Repository = "https://github.com/ALEXPJ02/faithful-transit-rag"

[tool.hatch.build.targets.wheel]
packages = ["src/transit_rag"]

# ---------------------------------------------------------------------------
# Lint + format (ruff replaces the reference repo's eslint + prettier pair)
# ---------------------------------------------------------------------------
[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "B",    # bugbear
    "UP",   # pyupgrade
    "SIM",  # flake8-simplify
    "RUF",  # ruff-specific
]
ignore = [
    "E501",  # line length is enforced by the formatter, not the linter
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["B011"]

[tool.ruff.format]
quote-style = "double"

# ---------------------------------------------------------------------------
# Type checking
# ---------------------------------------------------------------------------
[tool.mypy]
python_version = "3.11"
files = ["src", "tests"]
strict = false
warn_unused_ignores = true
warn_redundant_casts = true
warn_unreachable = true
disallow_untyped_defs = true
no_implicit_optional = true
show_error_codes = true

# Third-party packages without bundled type stubs.
[[tool.mypy.overrides]]
module = [
    "chromadb.*",
    "voyageai.*",
    "google.transit.*",
    "xgboost.*",
    "sklearn.*",
    "joblib.*",
    "ragas.*",
    "mcp.*",
]
ignore_missing_imports = true

# ---------------------------------------------------------------------------
# Tests
# ---------------------------------------------------------------------------
[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --strict-markers"
markers = [
    "live: hits a real external API (TfNSW / Anthropic / Voyage). Skipped by default.",
]
